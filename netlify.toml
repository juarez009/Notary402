[build]
  command = "npm ci --include=dev --prefer-online --no-audit --no-fund && npm run build:web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
  NPM_FLAGS = "--include=dev"
  SECRETS_SCAN_OMIT_KEYS = "AMOY_CHAIN_ID,ZAVU_DEFAULT_CHANNEL,VITE_NOTARY402_API_BASE_URL"
